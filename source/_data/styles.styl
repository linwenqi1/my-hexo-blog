.site-author-image {
  max-width: 64px;  // 改成你想要的大小，比如 64px、80px
  max-height: 64px;
}

// 行内代码样式
code {
  color: #476582;
  background-color: rgba(27, 31, 35, 0.05);
  padding: 2px 4px;
  border-radius: 3px;
  font-size: 90%;
}

// 代码块内的代码保持高亮器原样
pre code {
  color: inherit !important;
  background-color: transparent !important;
  padding: 0 !important;
  border-radius: 0 !important;
  font-size: inherit !important;
}